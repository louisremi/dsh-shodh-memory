# Local test scaffolding — never published.
node_modules/
.dsh-home/
.vendor-inspect/

# Ad-hoc boot logs from harness verification runs.
boot*.log
load-marker.json

# Local NAS access shim: ssh config + helper that reference a key outside this
# repo. Machine-specific and never published.
.sshcfg
/nas

# Editor / OS noise
.DS_Store
*.swp
